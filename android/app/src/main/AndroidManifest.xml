<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Membuat Rak Baca muncul di lembar "Bagikan" milik sistem.

              Ini menghapus gesekan terbesar aplikasi ini: membaca di peramban,
              lalu berpindah aplikasi dan mengetik ulang judul beserta
              tautannya. Sekarang cukup Bagikan, dan form terbuka terisi.

              Dibatasi text/plain — yang dikirim peramban saat membagikan
              halaman. Gambar dan berkas tidak ditangani; keduanya sudah punya
              jalur sendiri lewat pemilih galeri.

              `launchMode="singleTask"` sudah ada di atas, jadi membagikan saat
              aplikasi sedang berjalan memakai ulang instance yang sama dan
              masuk lewat onNewIntent, bukan membuka salinan baru.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Dibutuhkan @capacitor/camera untuk membaca gambar dari galeri. Plugin
      tidak menyuntikkan permission ini sendiri, jadi harus ditulis manual.

      READ_MEDIA_IMAGES untuk Android 13 (API 33) ke atas; di bawah itu Android
      memakai READ_EXTERNAL_STORAGE, yang dibatasi maxSdkVersion agar tidak
      ikut diminta di versi yang sudah tidak memerlukannya.

      CAMERA sengaja TIDAK ditambahkan: aplikasi ini hanya memilih dari galeri
      (CameraSource.Photos), tidak pernah memotret langsung. Menambahkannya
      hanya akan memunculkan permintaan izin yang tidak pernah dipakai.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
</manifest>
